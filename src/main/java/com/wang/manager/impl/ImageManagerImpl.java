package com.wang.manager.impl;

import com.wang.manager.Manager;

/**
 * ImageManagerImpl: image表操作
 * <p>
 * 访问范围: 全局
 * 调用者: Service
 * 异常检测: 确保DAO参数无差错
 * <p>
 * 1. 查询image queryImage(image_id)
 * 2. 插入image insertImage(Image)
 * 3. 获取全部images queryImages(user_id)
 * 4. 获取分类images queryClasses(class)
 *
 * @auther ten
 */
public class ImageManagerImpl implements Manager {


}
